import StripeRedirectCancel from "@/components/redirects/StripeRedirectCancel";
import PageWrapper from "@/components/wrapper/PageWrapper";

const PostStripeRedirectPage = () => {
  return (
    <PageWrapper>
      <StripeRedirectCancel />
    </PageWrapper>
  );
};

export default PostStripeRedirectPage;
