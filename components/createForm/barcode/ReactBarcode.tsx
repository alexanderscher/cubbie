import React, { useEffect } from "react";
import { toast } from "sonner";
("use client");
const ReactBarcode = () => {
  const [devices, setDevices] = React.useState([]);

  return <div>ReactBarcode</div>;
};

export default ReactBarcode;
