export interface CheckedReceipts {
  receipt_id: number;
  checked: boolean;
}

export interface CheckedProjects {
  project_id: number;
  checked: boolean;
}

export interface CheckedItems {
  item_id: number;
  checked: boolean;
}
